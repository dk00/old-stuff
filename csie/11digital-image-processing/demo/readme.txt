Extract all files to somewhere that php could be executed,
and modify .htaccess properly.

use sqlcmd.php for SQL commands, and 'select * from sqlite_master' 
for details. The default password for SQL is '123' without quotes.
To change the password, edit sqlcmd.php.
