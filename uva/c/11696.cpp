#include<cstdio>
using namespace std;
main() {
  int T;
  scanf("%d", &T);
  while (T--) {
    scanf("%d %d", &n, &m);
    for (i = 0; i < n; ++i)
      scanf("%d %d", x+i, y+i);
    for (i = 0; i < m; ++i)
      scanf("%d %d %d", x+n+i, y+n+i, r+i);
    for (i = 0; i < n; ++i)
      for (j = 0; j < m; ++j);

  }
}
