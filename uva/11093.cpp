#include<cstdio>
main()
{
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        for(i=0;i<n;i++)
            scanf("%d",&s[i]);
        for(i=now=min=0;i<n;i++)
        {
            scanf("%d",&j);
            now+=(s[i]-=j);
            min<?=now;
        }
        printf("Case %d: ",C++);
        if(sum<0)
        {
            puts("Not possible");
            continue;
        }
        for(i=0;i<n;i++)
        
    }
}
