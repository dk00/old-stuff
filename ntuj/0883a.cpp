#include<cstdio>
main() {
  int T, C = 1;
  scanf("%d", &T);
  while (T--) {
    int i, j, n, m, sum = 0;
    scanf("%d %d", &n, &m);
    for (i = 0; i < n; ++i) {
      scanf("%d", &j);
    }
    while (m--) {
      scanf("%d", &n);
      scanf("%d", &i);
      while (--n > 0) {
        scanf("%d", &j);
      }
      scanf("%d", &j);
    }
//    printf("Game #%d: %d\n", C++, sum);
  }
}
