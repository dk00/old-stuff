#include<queue>
#include<cstdio>

main()
{
    while(scanf("%d %d %d %d",&n,&m,&i,&t) && n)
    {
        for(j=0;j<n;j++)d[j]=inf;
        i--,t--;
        d[0]=0;
        h.clear();
        for(i=0;i<n;i++)
        {
            scanf("%d %d %d",&s[i].a,&s[i].b,&s[i].l);
            for(s[i].n=0;scanf("%d%c",&s[i].s[s[i].n++],tmp && tmp[0]!='\n';);
            
        }
    }
}
