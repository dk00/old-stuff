#include<ctime>
#include<cstdio>
#include<cstdlib>
main()
{
    int i,j,k;
    while(scanf("%d",&i)==1)
    {
        printf("%d\n",clock());
        for(i+=clock();i>=clock(););
        printf("%d\n",clock());
    }
}
