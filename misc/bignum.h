#define MAX 1000000
#define DLEN 10
const long long D1=100000*100000;
class bignum{
    long long s[MAX];
    long d;
    char *bigitoa(char *out);
    bigatoi(char *in);
    bignum operator* (bignum n);
    bignum operator= (bignum n);
    bignum operator+ (bignum n);
    bignum operator- (bignum n);
};
char *big::bigitoa(char *out){
}
